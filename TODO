courtesy of nice_burger on irc.freenode.net#rubyonrails

I18n,globalize2 for that. map.root in route.db to welcome or something. install haml, if only to 
                     get sass. freeze rails, action_mailer stuff for restful_auth, exception_notifier is good, capify, shoulda for 
                     tests, app_config plugin, put something nice in README, set up on github (ssh keys stuff to worry about). um, 
                     for starters.
